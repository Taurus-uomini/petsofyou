doctype html
html
head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible" content="ie=edge")
    title 登陆
    script(src="javascripts/require.js" data-main="javascripts/login")
    link(rel="stylesheet", href="/stylesheets/toolfun.css")
    style.
        #login_div
        {
            width: 50%;
            margin: 100px auto;
            text-align: center;
            padding: 50px;
            border: 1px solid rgba(0,0,0,.2);
            border-radius: 6px;
            box-shadow: 0 3px 9px rgba(0,0,0,.5);
        }
        #login_div input
        {
            display: inherit;
            margin: 30px auto;
            width: 50%;
            height: 20px;
        }
        #to_register
        {
            display: inherit;
            color: #416EAF;
            text-decoration: none;
            cursor: pointer;
            font-size: 10px;
            margin-top: 10px;
        }
body
    form(id="login_form")
        div(id="login_div")
            input(type="text" class="text_input" placeholder="用户名" name="username")
            input(type="password" class="text_input" placeholder="密码" name="password")    
            button(type="button" class="btn btn-primary" id="login_btn") 登陆
            a(id="to_register" href="/register") 没有账号？点击注册